import React from 'react';

// SpaceScene replaced with a light decorative background for the light mood redesign.
// Kept as a no-op component to avoid import errors in case it's referenced elsewhere.
export default function SpaceScene() {
  return null;
}
